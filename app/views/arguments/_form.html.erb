<%= form_with(model: argument) do |form| %>
  <% if argument.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(argument.errors.count, "error") %> prohibited this argument from being saved:</h2>

      <ul>
        <% argument.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :type, style: "display: block" %>
    <%= form.text_field :type %>
  </div>

  <div>
    <%= form.label :range_or_list, style: "display: block" %>
    <%= form.text_field :range_or_list %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
